#include "../header/menuGTK.h"
#include "../header/gameSDL.h"

int main (int argc, char **argv)
{
    s_menu *menuStruct;
    menuStruct = malloc(sizeof(s_menu));
    main_menu_gtk(argc,argv,menuStruct);
}